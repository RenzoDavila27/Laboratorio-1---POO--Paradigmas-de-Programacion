public interface Colocable{ //Esto lo hacemos por si en un futuro queremos implementar diferentes formas de posicionamiento de barcos
    void colocar(Jugador p1, Coordenadas position, int option);
}
